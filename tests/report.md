# IDF — QA Test Report

> Updated by QA after every test cycle. Each cycle gets a new entry — oldest entries stay for reference.
> Pass/fail is the final signal. Issues route to Researcher (content) or Fullstack Dev (technical).

---

## Cycle Log

| Cycle | Date | Intent | Result | Notes |
|---|---|---|---|---|
| — | — | — | — | Initial setup — no tests run yet |
| 1 | 2026-04-07 | Mobile responsiveness fixes | PASS | All checks passed: `.principles-grid` + `.flow-row` collapse in idf.html; `.principles-grid` collapse in playbook.html; `[style*="grid-template-columns:140px"]` override valid; `.scroll-hint` present before both diagram-outer divs in flow.html; `.section-contribute` class defined in CSS and applied in HTML with no inline padding; GA4 tag in `<head>` of all 4 pages; `nav.js?v=10` before `</body>` in all 4 pages; no hardcoded colors introduced |
| 2 | 2026-04-06 | Section 04 SDLC step cards for mobile | PASS | All structure checks passed: `.sdlc-step-grid` with `display:grid; grid-template-columns:repeat(5,1fr); gap:3px`; `.sdlc-step-card` as flex column; `.card-section-label` and `.card-section-content` defined; all 5 `highlight-*` modifiers present using CSS variables; `.flow-row` and `.flow-cell` fully removed; mobile media query uses `.sdlc-step-grid`. All 5 cards verified: correct labels (INPUT/OUTPUT, CHECKS/OUTPUT, GATE/OUTPUT, SIGNALS/OUTPUT), correct role subtitles, correct border-top color tokens, `color:var(--purple2)` on Craft Engineer + QA Advocate spans in Deploy card, `↻ next cycle begins` in Monitor OUTPUT. Regression checks passed: GA4 tag present; `nav.js?v=10` before `</body>`; all 4 fail-path callouts (FAIL PATH — auto-test, FAIL PATH — production, ESCAPE HATCH, GOVERNANCE) intact; SDD callout and Mermaid diagram present below fail paths. |
| 3 | 2026-04-06 | Playbook clarity — 18 coded reference replacements in playbook.html | PASS | All 18 instances verified. Instance 1: `WHEN TO RUN CLIENT SIGNAL TRIAGE` confirmed (line 766). Instances 2–3: `(W2)` removed from W1 opening paragraph and precondition callout (lines 642, 647). Instances 4–5: W1 scope sidebar `that is … (W2)` and `that is … (C1)` replaced with `see …` (lines 740–741). Instance 6: `(W1)` removed from W2 step 06 (line 846). Instances 7–8: W2 scope sidebar `(C1)` and `(W1)` removed (lines 858–859). Instance 9: `(W5)` removed from T4 staleness callout (line 1304). Instance 10: `<a href="#t4" style="color:var(--teal2);">System Memory template</a>` hyperlink confirmed (line 1514); `#t4` anchor exists (line 1125); no broken HTML. Instance 11: `(W1)` and `(C1)` removed from P2 step 04 (line 1541). Instance 12: `(R2)` inlined as `ambiguity halts execution (see Operating Principles)` and `(C1)` removed (line 1568). Instance 13: `(T3)` removed from P2 during-cycle table (line 1580). Instance 14: `IDF requires the PO to be the sole gate authority` confirmed (line 1231). Instance 15: `context pollution — stale context corrupts future agent decisions` confirmed (line 1235). Instance 16: `escalates immediately — ambiguity halts execution` confirmed (line 1409). Instance 17: `ADDRESSED — Cycle [number]` confirmed (line 829). Instance 18: `(C1)` removed from P2 Phase 3 retrospective artifact (line 1622). Scope checks: TOC `toc-id` spans untouched; section-label divs untouched; `## Cycle [N] — Intent` (line 716) and `## Gate Report — Cycle [N]` (line 973) use `[N]`. Regression: GA4 tag present (line 5); `nav.js?v=10` before `</body>` (line 1655). Notable finding (out of scope): one residual `(W1)` remains at line 767 (W2 precondition callout body) — not in the 18-instance list, flagged for Cycle 4 consideration. |
| 4 | 2026-04-07 | W4 Gate Review Decision — full section + residual (W1) fix | PASS | All checks passed. `<section id="w4">` contains full content — no COMING SOON. Opening paragraph present (Gate Review Decision, PO gate authority). PRECONDITION callout: coral border-left (`var(--coral)`), 3 conditions (gate report produced by Guardian, report linked to context, PO has Intent Statement). WHEN TO RUN callout: coral border-left, header `WHEN TO RUN GATE REVIEW DECISION`. `class="qs-steps"` wrapper present. Exactly 8 `.qs-step` divs (steps 01–08). Steps 01–07 each have `<div class="qs-line"></div>` before `<div class="qs-num">`. Step 08 has no `qs-line` (confirmed at line 984). Decision matrix in step 06: 3-column grid, 3 data rows — PASS (`var(--teal2)`), FLAG (`var(--amber2)`), FAIL (`var(--coral)`). PO decision record artifact in step 07: `qs-artifact-label` + `qs-artifact` divs present. Scope sidebar: `var(--surface2)` background, header `GATE REVIEW DECISION DOES NOT COVER`, 5 bullet entries. `(W1)` search returns zero results. W2 PRECONDITION callout body reads "Run Client Signal Triage before Intent Generation begins" (no parenthetical). TOC W4 entry untouched (line 595). `nav.js?v=10` before `</body>` (line 1778). GA4 tag `G-MV9XNW7Y6V` in `<head>` (line 5). |
| 5 | 2026-04-07 | W5 Context Reset — full section | PASS | All checks passed. `<section id="w5">` present with full content — no COMING SOON. Opening paragraph present (Context Reset, artifact hygiene, stale context). PRECONDITION callout: coral `border-left:3px solid var(--coral)`, 3 conditions (staleness trigger fired, PO approved, all in-scope artifacts accessible). WHEN TO RUN callout: coral border-left, header `WHEN TO RUN CONTEXT RESET`, 6 triggers including proactive 5–7 cycle cadence. `.qs-steps` wrapper present (line 1039). Exactly 7 `.qs-step` divs (steps 01–07, lines 1041–1095). Steps 01–06 each have `<div class="qs-line"></div>` before `<div class="qs-num">` (lines 1042, 1051, 1060, 1069, 1078, 1087). Step 07 (line 1095) has no `qs-line`. POST-RESET CHECKLIST: `qs-artifact-label` + `qs-artifact` present in step 07 with amber inline border style (`var(--amber)`), 7 checklist items. Scope sidebar: `var(--surface2)` background, header `CONTEXT RESET DOES NOT COVER`, 5 entries. TOC W5 entry intact (`<a href="#w5">Context Reset</a>`, line 596). `nav.js?v=10` before `</body>` (line 1881). GA4 tag `G-MV9XNW7Y6V` in `<head>` (line 5). |
| 6 | 2026-04-08 | W5 Context Reset reframe — emergency brake model, removed scheduled cadence | PASS | All 4 content rewrites verified. Opening paragraph (line 1017) starts with "Context Reset is the emergency brake" — correct. WHEN TO RUN callout (lines 1029–1033): exactly 5 triggers present; "Proactively every 5–7 cycles" is absent — correct. Step 01 qs-desc (line 1045) starts with "Identify the artifact closest to the failure" — correct. Step 07 qs-desc (line 1098) contains "The PO does not need to approve the reset before it begins" — correct. POST-RESET CHECKLIST: 7 checkbox items unchanged (lines 1100–1106). Regression: `nav.js?v=10` before `</body>` (line 1880); GA4 tag `G-MV9XNW7Y6V` in `<head>` (lines 5, 10). No structural changes detected. |
| 7 | 2026-04-08 | Gate Rituals framework changes in idf.html — A06 rule, signal events, role cards | PASS | All checks passed. Section 02: intro reads "Six rules" (not five); A06 block present immediately after R5 with text "Human gates validate alignment, not output." Section 06: Intent Injection output mentions Intent Alignment Gate and 30–60 minute read window; Craft Review output names Output Alignment Gate, Craft Engineer validates intent mechanism, QA Advocate validates client outcome; Context Reset cadence reads "Event-triggered" (scheduled cadence absent); Context Reset participants include "Craft Engineer (leads)"; Context Integrity Gate row present with `dot-execution` class, per-cycle cadence, Craft Engineer participant, output contains `Context check: CLEAN / FLAGGED` and `INTENT_LOG`. Section 03: Craft Engineer desc ends referencing Context Integrity Gate, Intent Alignment Gate, and Output Alignment Gate; QA Advocate desc ends referencing Intent Alignment Gate and Output Alignment Gate. Regression: `nav.js?v=10` before `</body>` confirmed; GA4 tag `G-MV9XNW7Y6V` in `<head>` confirmed. |
| 8 | 2026-04-08 | W3 Craft Review — Output Alignment Gate, 6-step procedure | PASS | All checks passed. Full section content present, no COMING SOON. Opening paragraph names "Output Alignment Gate" (line 874). PRECONDITION callout: coral border-left `var(--coral)` (line 877), references Guardian gate report prerequisite + all automated checks passed; solo practitioner exception absent — correct. WHEN TO RUN callout: coral border-left (line 883), header "WHEN TO RUN CRAFT REVIEW" (line 884), solo practitioner exception present (line 885). `.qs-steps` wrapper contains exactly 6 `.qs-step` divs (steps 01–06). Steps 01–05 each have `<div class="qs-line"></div>`; step 06 omits it — correct. Step 05 has `qs-artifact` with FLAG template "FLAGGED by [Craft Engineer / QA Advocate]: [what the output does] — intent specified [what it should do]." Step 06 has `qs-artifact` with APPROVED template pairing "Craft Engineer: Mechanism matched:" and "QA Advocate: Client outcome confirmed:". Maturity graduation callout present with amber inline border `var(--amber)` (line 953). Scope sidebar: `var(--surface2)` background (line 959), header "CRAFT REVIEW DOES NOT COVER" (line 960). TOC entry `<span class="toc-id">W3</span>Craft Review` intact (line 594). Regression: `nav.js?v=10` before `</body>` (line 1970); GA4 tag `G-MV9XNW7Y6V` in `<head>` (lines 5, 10). |
| 9 | 2026-04-08 | W1 update — Intent Alignment Gate step (Gate Ritual 1) | PASS | All checks passed. W1 section (`<section id="w1">`) contains exactly 7 `.qs-step` divs (lines 654, 663, 672, 681, 697, 711, 735). Steps 01–06 each have `<div class="qs-line"></div>` (confirmed at lines 655, 664, 673, 682, 698, 712). Step 07 (line 735) omits `qs-line` — correct. Step 07 title: "Open the Intent Alignment Gate" (line 738). Step 07 body: 30–60 minute window present (line 739); Craft Engineer and QA Advocate roles named in role grid (lines 742–743); CONCERN format present — "CONCERN — [Craft Engineer / QA Advocate]: [one sentence stating the blocker]" (line 746); "Silence = PASS" principle present in coral NOTE callout (line 748). Coral NOTE callout present: `border-left:3px solid var(--coral)` inline style with "NOTE — " label (line 747–748). Amber maturity graduation callout present (lines 751–754): header "MATURITY GRADUATION — INTENT ALIGNMENT GATE"; mentions 90% first-pass rate above 90% for 10 consecutive cycles. Regression: GA4 tag `G-MV9XNW7Y6V` in `<head>` (lines 5, 10); `nav.js?v=10` before `</body>` (line 2112). |
| 10 | 2026-04-08 | W8 Context Integrity Gate — new section (Gate Ritual 2) | PASS | All checks passed. `<section id="w8">` at line 1261 — full content present, no COMING SOON within the section (COMING SOON lines 1246 and 1256 belong to W6 and W7 respectively). TOC entry present: `<span class="toc-id">W8</span>Context Integrity Gate` (line 599). Section label: "W8 — Workflows" (line 1262). WHEN TO RUN callout present with coral border-left `var(--coral)` (lines 1267–1270), header "WHEN TO RUN". "THIS IS NOT THE CONTEXT RESET" callout present with coral border-left and explicit distinction label (lines 1273–1276). PRE-CYCLE CONTEXT CHECK `qs-artifact` present in step 04 (lines 1314–1323). INTENT_LOG context check field format present in step 05 (lines 1333–1336): "Context check: CLEAN / FLAGGED" pattern confirmed. Maturity graduation callout: amber border `border-left:3px solid var(--amber)` (line 1360), mentions 20 consecutive clean cycles (line 1362). Scope sidebar: `background:var(--surface2)` (line 1366). Regression: GA4 tag `G-MV9XNW7Y6V` in `<head>` (lines 5, 10); `nav.js?v=10` before `</body>` (line 2112). |
| 11 | 2026-04-08 | W6 Escape Hatch Protocol + C4 Orchestrator Escalation Messages: full sections | PASS | All checks passed. W6: full content present (no COMING SOON); TWO PATHS callout present with PATH A — INTERRUPT and PATH B — COMPLETE AND LOG cards (lines 1254, 1259); decision table has header row + 7 data rows (lines 1270–1283); 5 qs-step divs at lines 1293/1302/1311/1324/1346; steps 01–04 each have qs-line, step 05 (line 1346) omits qs-line; CORRECTION STATEMENT artifact present (lines 1317–1320); DRIFT REGISTER escape entry present (lines 1330–1342); INTENT_LOG `Escape:` field present (line 1352); scope sidebar present (lines 1359–1367). C4: full content present (no COMING SOON); ESCALATION format with 6 fields (lines 2044–2050); RESPONSE format with 4 fields including `Proceed: YES / NO` (lines 2089–2094); SLA callout with amber border `var(--amber)`, mentions 2-hour and 30-minute defaults (lines 2097–2100); 4 qs-step divs at lines 2105/2117/2129/2138; steps 01–03 each have qs-line, step 04 (line 2138) omits qs-line; anti-patterns table present (lines 2063–2084); scope sidebar present (lines 2156–2164). Regression: `nav.js?v=10` before `</body>` (line 2357); GA4 tag `G-MV9XNW7Y6V` in `<head>` (lines 5, 10); TOC W6 entry at line 597; TOC C4 entry at line 622 — all intact. |
| 12 | 2026-04-08 | Gate Maturity Model — idf.html Section 09 callout + P3 full section | PASS | All checks passed. idf.html Section 09: three-stage grid present (Stage 1 Structured / Stage 2 Calibrated / Stage 3 Autonomous) at lines 2071–2090; GRADUATION IS NOT AUTOMATIC callout with cross-reference link `playbook.html#p3` present (line 2094); `nav.js?v=10` before `</body>` (line 2308); GA4 `G-MV9XNW7Y6V` in `<head>` (lines 5, 10). playbook.html P3: full content present — no COMING SOON; TOC entry reads "Gate Maturity Model" (line 631); stage summary table has header row + 3 data rows (Structured / Calibrated / Autonomous) at lines 2356–2377; individual graduation criteria for G1 (Intent Alignment Gate, >90% for 10 consecutive cycles), G2 (Output Alignment Gate, >95% for 15 cycles AND no drift escapes for 20 cycles), G3 (Context Integrity Gate, no Context Reset for 20 cycles) all present at lines 2383–2423; 3-step graduation process present including System Memory log template (lines 2429–2465); "What never graduates" table has 4 rows — PO Release Gate (W4), Escalation response obligation (C4), Context Reset trigger (W5), Craft Engineer output review (Output Alignment Gate step 02 in W3) — at lines 2476–2496; cross-reference sidebar links to idf.html#measurement + W1, W3, W4, W5, W8 gate sections (lines 2499–2509); `nav.js?v=10` before `</body>` (line 2519); GA4 `G-MV9XNW7Y6V` in `<head>` (lines 5, 10). |
| 13 | 2026-04-08 | Section 06 visual redesign — three-zone signal events (Cycle View, Rhythm Map, Exception Events + reference table) | PASS | All checks passed. Zone 1 — Cycle View: `.cycle-view` wrapper with `overflow-x:auto` present (line 957); `.cycle-track` grid present with 6 columns `120px 130px 160px 1fr 160px 140px` (line 960); column order correct — G2 Context Integrity Gate → Intent Injection → G1 Intent Alignment Gate → Agent Execution band → G3 Craft Review → Release Gate (lines 1952–2016); G1/G2/G3 `.gate-ritual-badge` spans present (lines 1956, 1971, 2001); agent execution band has `.cycle-col-agent` with `background:rgba(74,122,191,0.08)` blue tint (lines 982–989); directional indicator present as `.cycle-time-arrow` with `minutes` left label / `days` right label above the track (lines 1947–1951); "Every Cycle" label present as `.zone-header` (line 1943). Zone 2 — Rhythm Map: `.rhythm-map` wrapper present (line 2026); three rows — "Every Cycle" (line 2029), "Continuous" (line 2059), "On Signal" (line 2077); "Every Cycle" row has 6 chips (≥5 required — Context Integrity Gate, Intent Injection, Intent Alignment Gate, Quality Gate, Craft Review, Release Gate); "On Signal" row has 3 chips all with `.rhythm-chip-exception` dashed border class (lines 2079, 2083, 2087); exception footnote present at line 2095. Zone 3 — Exception Events: `.exception-grid` present (line 2100) with exactly 3 cards — Context Reset (line 2102), Capability Harvest (line 2122), Dependency Sync (line 2142); each card has Trigger + Owner + Output rows. Reference table: `ref-divider` label "Full Reference — all 11 signal events" present (line 2166); exactly 11 `.ritual-row` entries confirmed. All 11 events verified: Strategic Alignment ✓, Intent Injection ✓, Release Gate ✓, Context Reset ✓, Context Integrity Gate ✓, Capability Harvest ✓, Quality Gate ✓, Craft Review ✓, Drift Check ✓, Dependency Sync ✓, Client Signal Review ✓. Regression: `nav.js?v=10` before `</body>` (line 2752); GA4 `G-MV9XNW7Y6V` in `<head>` (lines 5, 10); all 4 new CSS classes present in `<style>` (`.cycle-view` line 957, `.cycle-track` line 958, `.rhythm-map` line 1037, `.exception-grid` line 1077); inline colors in new HTML use only `var(--*)` and `rgba(*)` — no hardcoded hex values. |

---

## Test Checklist (run each cycle)

### Visual / Rendering
- [ ] All pages render correctly on desktop (1280px+)
- [ ] All pages render correctly on mobile (375px)
- [ ] No broken layouts or overflow issues
- [ ] CSS variables used throughout — no hardcoded colors

### Navigation
- [ ] `nav.js` loads and sticky nav displays correctly on all pages
- [ ] All nav links resolve correctly
- [ ] Version badge displays correct version

### Content
- [ ] No placeholder or TODO text visible
- [ ] No broken internal links
- [ ] External links open in new tab with rel="noopener"

### Analytics
- [ ] GA4 tag (G-MV9XNW7Y6V) present in `<head>` of all active pages

### Performance
- [ ] Pages load without console errors
- [ ] No missing font or asset 404s

### Accessibility
- [ ] Heading hierarchy is logical (h1 → h2 → h3)
- [ ] Links have descriptive text (no bare URLs as link text)

---

## Issue Log

| ID | Cycle | Type | Description | Routed to | Status |
|---|---|---|---|---|---|
| — | — | — | — | — | — |
